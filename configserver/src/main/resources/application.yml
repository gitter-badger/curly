server:
 port: 10004

eureka:
  client:
    registryFetchIntervalSeconds: 5
    serviceUrl:
      defaultZone: http://localhost:10003/eureka/
  instance:
    leaseRenewalIntervalInSeconds: 10

spring:
  cloud:
    config:
      server:
        git:
          uri: git@github.com:crly/curly-config.git


security:
  user:
    password: nessa
