apply plugin: 'groovy'
apply plugin: 'spring-boot'

dependencies {
    compile "org.codehaus.groovy:groovy-all:$groovyVersion"
    compile "org.springframework.cloud:spring-cloud-config-server"
}
jar {
    baseName = 'config-server'
}
springBoot {
    mainClass = 'curly.ConfigServerApplication'
}
