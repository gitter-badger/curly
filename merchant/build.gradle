apply plugin: 'spring-boot'
apply plugin: 'groovy'

dependencies {
    compile "org.codehaus.groovy:groovy-all:$groovyVersion"
    compile "org.springframework.data:spring-data-gemfire"
    compile project(":curly-commons")
}
jar {
    baseName = 'merchant'
}
springBoot {
    mainClass = 'curly.SmugglerApplication'
}

